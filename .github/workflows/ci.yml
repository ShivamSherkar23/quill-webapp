name: CI/CD

on:
  pull_request:
    branches:
      - master
  push:
    branches:
      - master
    tags:
    - "v*.*.*"
  workflow_dispatch:


permissions:
  contents: read
  packages: write

env:
  PYTHON_VERSION: "3.12"
  DOCKER_PLATFORMS: linux/amd64,linux/arm64

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest

    steps:
    - &checkout
      name: Checkout
      uses: actions/checkout@v6

    - &setup-python
      name: Set up Python
      uses: actions/setup-python@v6
      with:
        python-version: ${{ env.PYTHON_VERSION }}

    - &setup-uv
      name: Set up uv
      uses: astral-sh/setup-uv@v7
      with:
        enable-cache: true

    - &setup-task
      name: Set up Task
      uses: go-task/setup-task@v2

    - &install-deps
      name: Install dependencies
      run: task install

    - name: Run Ruff checks
      run: task lint

  test:
    name: Test
    runs-on: ubuntu-latest
    needs:
      - lint

    steps:
    - *checkout
    - *setup-python
    - *setup-uv
    - *setup-task
    - *install-deps

    - name: Run tests
      run: task test

  coverage:
    name: Coverage
    runs-on: ubuntu-latest
    needs:
      - test

    steps:
    - *checkout
    - *setup-python
    - *setup-uv
    - *setup-task
    - *install-deps

    - name: Run coverage
      run: task coverage

    - name: Upload coverage report
      uses: actions/upload-artifact@v7
      with:
        name: coverage-report
        path: coverage.xml

  docker:
    name: Docker Build and Push
    runs-on: ubuntu-latest
    needs:
      - coverage
    if: github.event_name == 'push' && (github.ref_name == 'master' || startsWith(github.ref, 'refs/tags/v'))

    steps:
    - *checkout

    - name: Set up QEMU
      uses: docker/setup-qemu-action@v4

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v4

    - *setup-python

    - name: Prepare Docker metadata inputs
      id: docker-vars
      run: |
        APP_VERSION="$(python -c 'import tomllib; print(tomllib.load(open("pyproject.toml", "rb"))["project"]["version"])')"
        echo "image_name=ghcr.io/${GITHUB_REPOSITORY,,}" >> "$GITHUB_OUTPUT"
        echo "app_version=${APP_VERSION}" >> "$GITHUB_OUTPUT"

    - name: Log in to GitHub Container Registry
      if: github.event_name == 'push' && (github.ref_name == github.event.repository.default_branch || startsWith(github.ref, 'refs/tags/v'))
      uses: docker/login-action@v4
      with:
        registry: ghcr.io
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}

    - name: Extract Docker metadata
      id: meta
      uses: docker/metadata-action@v6
      with:
        images: ${{ steps.docker-vars.outputs.image_name }}
        tags: |
          type=raw,value=${{ steps.docker-vars.outputs.app_version }}-dev.${{ github.run_number }}
          type=sha,prefix=sha-

    - name: Build and push Docker image
      uses: docker/build-push-action@v7
      with:
        context: .
        target: runtime
        platforms: ${{ env.DOCKER_PLATFORMS }}
        push: ${{ github.event_name == 'push' && (github.ref_name == github.event.repository.default_branch || startsWith(github.ref, 'refs/tags/v')) }}
        tags: ${{ steps.meta.outputs.tags }}
        labels: ${{ steps.meta.outputs.labels }}
        provenance: false
        cache-from: type=gha
        cache-to: type=gha,mode=max
